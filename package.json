{
  "name": "DD Tools",
  "version": "0.0.1",
  "description": "I am DD",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . DDTools --out Build --overwrite --platform=linux --arch=x64 --icon=icon.ico --prune=true --squirrel-install --ignore=node_modules/electron-* --electron-version=11.2.0"
  },
  "keywords": [],
  "author": "GitHub",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.2.0",
    "electron-packager": "^15.2.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "querystring": "^0.2.0"
  }
}
